/// ShikiKit — shared types, DTOs, WebSocket messages, and error types
/// for the Shiki platform. Imported by both ShikiServer and ShikiApp.
public enum ShikiKit {
    public static let version = "4.0.0"
}
